@model FileDrop.Web.Models.Layout.UserInfoViewModel

@if (Model.IsLoggedIn)
{
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <span>Hello, @Model.UserName</span>
            <b class="caret"></b>
        </a>
        <ul class="dropdown-menu">
            <li><a href="@Url.Action("Logout","Account")">Logout</a></li>
        </ul>
    </li>
}

